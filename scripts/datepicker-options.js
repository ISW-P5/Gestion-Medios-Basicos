// Initialize Bootstrap DatePicker -> only html input
$(".datepicker").datepicker({
    todayBtn: "linked",
    weekStart: 1,
    todayHighlight: true,
    autoClose: true,
    orientation: "bottom",
    disableTouchKeyboard: true,
    clearBtn: true,
});